[develop]
script_dir=$base/lib/go2_navigation
[install]
install_scripts=$base/lib/go2_navigation

[flake8]
max-line-length = 99

[tool:pytest]
testpaths = test
# 当前主机用户目录安装了与 Ubuntu 22.04 系统 pytest 不兼容的 anyio 插件。
# 本包不使用 anyio，显式禁用它可使直接 pytest 与 colcon test 结果一致。
addopts = -p no:anyio
