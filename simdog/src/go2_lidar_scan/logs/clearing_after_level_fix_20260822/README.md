# 重力对齐后的 clearing 复测

- 完整在线 SLAM，`map_session:=new`，默认 `/scan` 为 `velodyne_level`；
- 正前方 2.0 m、`0.3×0.3×0.5 m` 方块，`scan` 10/10 帧检出且无接触；
- 保留方块时 Local Costmap `cost=254` 格为 155；
- 通过 `/delete_entity` 实际删除后，连续 8 次采样为
  `146, 148, 148, 147, 149, 146, 146, 147`。

删除后的数值回到 146–149 背景带，本次判定 clearing PASS。该结果证明重力对齐没有
破坏 `+inf` 清除语义；它仍只是单位置冒烟测试，不替代多位置和 10 分钟压力验收。
